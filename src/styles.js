import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    background: {
        backgroundColor: '#112e51',
        minWidth: '100vw',
        width: '100%',
        // maxWidth: '100%',
        height: '100%',
        minHeight: '100vh'
    }
}));
